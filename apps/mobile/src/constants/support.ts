/**
 * Contact address for support, GDPR requests and ban appeals.
 *
 * Delivered by Cloudflare Email Routing on locastar.se, which forwards to
 * whichever inbox is actually monitored. That indirection is the point: the
 * published address stays the same even if the inbox behind it changes, so
 * nothing here or in the legal pages has to be reissued.
 *
 * Every screen and legal page reads this constant — don't hardcode it.
 */
export const SUPPORT_EMAIL = 'support@locastar.se';
